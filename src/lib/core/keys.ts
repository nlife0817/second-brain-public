// Имена ключей хранения, общие для сервера и браузера.
//
// Файл намеренно без импортов: его тянут и серверные модули (`bootstrap.ts`
// читает cookie), и клиентские (`ui-store.ts` её пишет). Любая зависимость
// здесь утекла бы в оба бандла сразу.

/**
 * Активная организация. Именно cookie, а не localStorage: сервер обязан знать,
 * какую организацию рендерить, до того как в браузере выполнится первый скрипт.
 */
export const ACTIVE_ORG_COOKIE = "sb_v2_org";

/** Прежнее место хранения активной организации — читается один раз при переезде. */
export const ACTIVE_ORG_LEGACY_KEY = "sb.v2.orgId";

/** Год: выбор организации — долгоживущая настройка, а не сессионная. */
export const ACTIVE_ORG_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
